const BlockEmbed = Quill.import('blots/block/embed');class FileBoxBlot extends BlockEmbed {  static blotName = 'fileBox';  static tagName = 'div';  static className = 'file-box';  static create(value) {    const node = super.create();    node.innerHTML = `      <img class="file-icon" src="${value.src}" alt="파일아이콘">      <span class="file-name">${value.name}</span>    `;    return node;  }  static value(node) {    const img = node.querySelector('.file-icon');    const span = node.querySelector('.file-name');    return {      src: img?.getAttribute('src') || '',      name: span?.textContent || '',    };  }}Quill.register(FileBoxBlot);